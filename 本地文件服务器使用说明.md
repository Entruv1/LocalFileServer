# 本地文件服务器 — 使用说明

> 目的：路由器无法直接传文件，用它在本地电脑起一个 HTTP 文件服务器，
> 路由器通过 `wget` 直接抓取 `mt5700-fix.sh` 等脚本，免去 SSH 粘贴几百行。

## 一、启动

双击 `本地文件服务器.exe`（绿色单文件，免安装）。

## 二、配置

| 项 | 说明 | 默认 |
|---|---|---|
| 监听端口 | 路由器访问的端口 | 8848 |
| 本机 IP | 自动列出电脑所有网卡 IP，选连路由器的那个 | 自动 |
| 访问口令 | 可选，留空则无口令 | 空 |
| 共享目录 | 放待抓取脚本的文件夹 | 工作区目录（脚本已就位） |

点 **启动服务** 后，窗口显示访问地址（如 `http://192.168.66.149:8848`）。

## 三、路由器抓取

在路由器 SSH 里执行（**替换成你电脑的实际 IP 和端口**）：

```sh
# 一键修复脚本（AT 通道 + 实时网速 + 信号上报 三合一）
wget -O /root/mt5700-fix.sh http://192.168.66.149:8848/mt5700-fix.sh
sh /root/mt5700-fix.sh

# 或只抓 speedd.sh
wget -O /root/atbridge/speedd.sh http://192.168.66.149:8848/speedd.sh
```

## 四、网页界面

浏览器打开 `http://192.168.66.149:8848/`（或点窗口"在浏览器打开"）：
- 列出共享目录所有文件
- 每个文件有一键复制的 `wget` 命令
- 可上传（拖拽/点击）、删除文件
- 设置了口令时，抓取命令自动带 `?token=xxx`

## 五、注意

- 只监听局域网；如需加密访问，在窗口填"访问口令"
- 若电脑与路由器**不在同一网段**，路由器无法 `wget` 到电脑，需先解决网络（同连一个 LAN）
- 改了共享目录里的文件，网页点"刷新"即可看到
